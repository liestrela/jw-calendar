# jw-calendar

*Quick and dirty* scraper that fetches class schedules from USP's JupiterWeb and exports a RFC 5545 compliant (iCalendar) calendar file.

## Usage:

Set `ID_USP` and `PASS_USP` environment variables (and `PROGRAM_NO` if you've done multiple programs) accordingly and run `jw-calendar.py`.
